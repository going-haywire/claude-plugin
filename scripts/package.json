{
  "name": "@going-haywire/claude-plugin-scripts",
  "version": "0.1.1",
  "private": true,
  "description": "Deterministic pre-studio scripts for the haywire Claude Code plugin — doctor (prereq probe), studioctl (sidecar-aware studio lifecycle), bootstrap (haywire init wrapper), permissions (optional farmhand tool-wildcard allow-rule). No MCP; the onboarding skills orchestrate these.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run build:test && node dist-test/doctor.test.js && node dist-test/studioctl.test.js && node dist-test/studioctl.launch.test.js && node dist-test/bootstrap.test.js && node dist-test/permissions.test.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
